var R = require('ramda')

describe('convert_to_blocks', () => {  
    var char_key = require('../front-end/js/char_key.js')

    it('should be an object',
        () => expect(typeof char_key).toEqual('object'))

})
